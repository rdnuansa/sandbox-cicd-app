{
  "name": "sandbox-cicd-app",
  "version": "1.0.0",
  "description": "Minimal Node.js web app for CI/CD learning sandbox",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "test": "node src/server.js & sleep 3 && curl -f http://localhost:3000 && pkill -f 'node src/server.js'"
  },
  "keywords": ["cicd", "docker", "express", "sandbox"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
