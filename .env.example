# Docker Compose Configuration

# Registry settings (use ghcr.io for GitHub Container Registry or docker.io for Docker Hub)
REGISTRY_URL=ghcr.io/rdnuansa
IMAGE_NAME=sandbox-cicd-app
IMAGE_TAG=latest

# Port mapping
HOST_PORT=8080
CONTAINER_PORT=3000

# Application settings
NODE_ENV=production
